# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env / secrets
.env

# Personal documents (CV has private contact info) — never push to a public repo
*.docx
*_CV*.pdf

# Local data you may not want to publish (keep samples in data/, ignore the rest)
data/private/

# Real corpus: copyrighted papers + theses, ~200MB — used locally, never pushed.
# Ignore the folder's contents but keep the title-list screenshot (just titles,
# no paper content) as evidence of what the corpus contains.
literature_review/*
!literature_review/Screenshot.png
ocr_text/

# Claude Code session logs
claude-logs/
claude_skip_permissions.sh

# Model cache (sentence-transformers downloads here)
.cache/
